name: Build and Release

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v4
    
    - name: Setup MSBuild
      uses: microsoft/setup-msbuild@v2
    
    - name: Restore NuGet packages
      run: nuget restore ScpReroll/ScpReroll.csproj
    
    - name: Build with MSBuild
      run: msbuild ScpReroll/ScpReroll.csproj /p:Configuration=Release /p:Platform="Any CPU"
    
    - name: Upload DLL to Release
      uses: softprops/action-gh-release@v1
      with:
        files: ./ScpReroll/bin/Release/net48/ScpReroll.dll
        tag_name: v${{ github.run_number }}
        name: Release v${{ github.run_number }}
        body: |
          Automated build from commit ${{ github.sha }}
          
          Download the DLL below and place it in your plugins folder.
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
